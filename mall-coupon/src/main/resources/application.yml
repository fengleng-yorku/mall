spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/mall_sms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: mall_user
    password: StrongPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#      config:
#        server-addr: 127.0.0.1:8848
#        namespace: bb255516-84ae-4ca0-aad8-09db0c14cafd
  application:
    name: mall-coupon
#  config:
#    import:
#      - nacos:datasource.yml?group=dev&refreshEnabled=true
#      - nacos:mybatis.yml?group=dev&refreshEnabled=true
#      - nacos:other.yml?group=dev&refreshEnabled=true

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 7001
